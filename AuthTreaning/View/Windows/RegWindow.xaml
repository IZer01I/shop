<Window x:Class="AuthTreaning.View.Windows.RegWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AuthTreaning.View.Windows"
        mc:Ignorable="d"
        xmlns:viewmodel="clr-namespace:AuthTreaning.ViewModel"
        Title="RegWindow" Height="450" Width="800">
    <Window.DataContext>
        <viewmodel:RegViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBox Grid.Row="0" Width="250" Height="50" Text="{Binding Login}"/>
        <TextBox Grid.Row="1" Width="250" Height="50" Text="{Binding Password}"/>
        <TextBox Grid.Row="2" Width="250" Height="50" Text="{Binding UserName}"/>
        <TextBox Grid.Row="3" Width="250" Height="50" Text="{Binding Mail}"/>
        <Button Grid.Row="4" Width="150" Height="50" Command="{Binding RegisterUserCommand}"/>
    </Grid>
</Window>
